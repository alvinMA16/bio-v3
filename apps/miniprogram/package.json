{
  "name": "@bio/miniprogram",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "dev": "tsc -p tsconfig.json --noEmit --watch",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test"
  },
  "devDependencies": {
    "miniprogram-api-typings": "^4.0.8",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "@bio/contracts": "workspace:*"
  }
}
